% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legend-spec.R
\name{legend_subset}
\alias{legend_subset}
\title{Select content from a composite legend specification}
\usage{
legend_subset(
  spec,
  grid_rows = NULL,
  keys = NULL,
  symbols = NULL,
  key_columns = NULL,
  group_gap = spec$group_gap,
  group_title_nudge = spec$group_title_nudge,
  xlim = spec$xlim
)
}
\arguments{
\item{spec}{A \code{\link[=legend_spec]{legend_spec()}} object.}

\item{grid_rows}{Grid row positions to keep, in display order. \code{NULL} keeps
every row.}

\item{keys}{Names of colour keys to keep, in display order. \code{NULL} keeps all.}

\item{symbols}{Symbol labels to keep, in display order. \code{NULL} keeps all.}

\item{key_columns}{Number of colour-key columns in the result. When \code{keys}
is supplied, the default is one; otherwise the original count is kept.}

\item{group_gap, group_title_nudge, xlim}{Optional layout overrides. Each
inherits from \code{spec} when omitted. Set \code{xlim = NULL} to recompute the
horizontal limits for the selected content.}
}
\value{
A new \code{ggpop_legend_spec} object.
}
\description{
Creates a new specification without changing \code{spec}. Grid rows are
renumbered from one so the selected grid stays compact. Render the result
with \code{\link[=legend_render]{legend_render()}} after selecting its final content.
}
\examples{
grid <- data.frame(row = c(1, 2), col = c(1, 1),
                   icon = c("square-inset", "circle-solid"),
                   label = c("A", "B"))
spec <- legend_spec(grid, keys = c(One = "#1090F3", Two = "#DF4601"))
legend_subset(spec, grid_rows = 1, keys = "One")
}
\seealso{
\code{\link[=legend_spec]{legend_spec()}}, \code{\link[=legend_add_key]{legend_add_key()}}, \code{\link[=legend_render]{legend_render()}}
}
