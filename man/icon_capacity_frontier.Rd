% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icon-capacity-frontier.R
\name{icon_capacity_frontier}
\alias{icon_capacity_frontier}
\title{Build an icon frontier panel with a capacity threshold}
\usage{
icon_capacity_frontier(
  data,
  x,
  y,
  icon,
  colour,
  frontier,
  label,
  bold,
  palette,
  capacity,
  label_sets,
  capacity_label = NULL,
  capacity_label_offset = 60,
  deliverable_label = NULL,
  x_expand = c(0.03, 0.08),
  y_expand = c(0, 0.04),
  xlab = NULL,
  ylab = NULL,
  icon_size = 1.4,
  dpi = 120,
  line_width = 0.6,
  label_size = 3.1,
  x_breaks = scales::breaks_pretty(8),
  y_breaks = scales::breaks_pretty(8),
  y_labels = scales::label_number(big.mark = ","),
  band_fill = "grey93",
  capacity_colour = "grey30",
  segment_colour = "grey55",
  plot_theme = NULL
)
}
\arguments{
\item{data}{A nonempty data frame.}

\item{x, y}{Numeric columns for the horizontal and vertical axes.}

\item{icon}{Column of icon names accepted by \code{\link[=geom_icon_point]{geom_icon_point()}}.}

\item{colour}{Column whose values are names in \code{palette}.}

\item{frontier}{Logical column or expression selecting the line rows.}

\item{label}{Column or expression containing text labels.}

\item{bold}{Logical column or expression selecting bold labels.}

\item{palette}{Named character vector of colours.}

\item{capacity}{Positive numeric threshold on the y axis.}

\item{label_sets}{A list of lists. Each inner list has a logical \code{rows}
vector selecting labels and may include arguments to
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}, such as \code{nudge_y}, \code{direction}, or \code{ylim}.}

\item{capacity_label}{Text placed just below the threshold at the leftmost
x value. \code{NULL} omits it.}

\item{capacity_label_offset}{Vertical distance below \code{capacity} for that
label.}

\item{deliverable_label}{Italic text placed halfway up the capacity band.
\code{NULL} omits it.}

\item{x_expand, y_expand}{Length-two multiplicative axis expansions.}

\item{xlab, ylab}{Axis titles.}

\item{icon_size, dpi}{Icon size and rendering resolution.}

\item{line_width, label_size}{Frontier line width and direct-label size.}

\item{x_breaks, y_breaks}{Break functions for each axis.}

\item{y_labels}{Label function for the y axis.}

\item{band_fill}{Fill colour below the capacity threshold.}

\item{capacity_colour}{Colour of the threshold and its annotation.}

\item{segment_colour}{Colour of label-repulsion segments.}

\item{plot_theme}{A ggplot theme. The default uses a 13-point classic theme
with black axis text.}
}
\value{
A ggplot.
}
\description{
Draws a capacity band and threshold behind the frontier line and icon
points. Each entry in \code{label_sets} selects rows and supplies its own
arguments to \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}, while the common label styling
stays consistent. The input data determines which rows are on the frontier.
}
\examples{
df_data <- data.frame(
  effect = 1:3, demand = c(100, 200, 350),
  icon = c("square-inset", "circle-hollow", "diamond-solid"),
  group = c("A", "B", "C"), frontier = c(TRUE, TRUE, FALSE),
  label = c("A", "B", "C"), bold = c(FALSE, TRUE, FALSE)
)
icon_capacity_frontier(
  df_data, x = effect, y = demand, icon = icon, colour = group,
  frontier = frontier, label = label, bold = bold,
  palette = c(A = "#1090F3", B = "#DF4601", C = "#08B8A2"),
  capacity = 250, label_sets = list(list(rows = df_data$frontier)),
  capacity_label = "Capacity", dpi = 120
)
}
\seealso{
\code{\link[=icon_frontiers]{icon_frontiers()}}
}
