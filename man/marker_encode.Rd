% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marker-encode.R
\name{marker_encode}
\alias{marker_encode}
\title{Encode data values as icon marker names}
\usage{
marker_encode(start, stop, once = FALSE, none = FALSE, scheme = "sda2028")
}
\arguments{
\item{start, stop}{Vectors of values used to select shape family and variant.
Numeric, character, and factor values are accepted. Values may be missing
on rows selected by \code{once} or \code{none} when they are not needed.}

\item{once, none}{Logical vectors marking one-time and no-screening rows.
Scalars are recycled. The two flags cannot both be \code{TRUE} on a row.}

\item{scheme}{\code{"sda2028"} or a list with named character vectors \code{start},
\code{stop}, and \code{once}, plus one character scalar \code{none}. The \code{start} and
\code{stop} values are marker-name parts; \code{once} and \code{none} are complete icon
names. Schemes may use bundled markers or user SVG names.}
}
\value{
A character vector of icon names, one per input row.
}
\description{
A marker scheme maps one value to a shape family and another to a variant.
Regular markers combine the two names with a hyphen. One-time values map
directly to a marker, while \code{none} uses the scheme's fallback marker.
}
\details{
The built-in \code{"sda2028"} scheme uses screening start ages 45/50/55 for
square/circle/diamond and stop ages 70/75/80/85 for
inset/hollow/cross/solid. One-time screening uses solid markers at ages
45/50/55, \code{"plus-bold"} at 60, and \code{"triangle-down"} at 65.
}
\examples{
marker_encode(c(45, 50, NA), c(70, 85, NA), none = c(FALSE, FALSE, TRUE))
marker_encode(c(45, 60), c(NA, NA), once = TRUE)

other <- list(
  start = c(A = "square"), stop = c(B = "hollow"),
  once = c(A = "square-solid"), none = "circle-solid"
)
marker_encode("A", "B", scheme = other)
}
\seealso{
\code{\link[=ggpop_markers]{ggpop_markers()}}, \code{\link[=geom_icon_point]{geom_icon_point()}}
}
