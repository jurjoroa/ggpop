% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icon-frontiers.R
\name{icon_frontiers}
\alias{icon_frontiers}
\title{Build matching icon frontier panels from named datasets}
\usage{
icon_frontiers(
  data,
  x,
  y,
  icon,
  colour,
  frontier,
  label,
  bold,
  palette,
  label_nudge = c(0.012, -0.018),
  x_expand = c(0.03, 0.16),
  y_labels = scales::label_number(big.mark = ","),
  xlab = NULL,
  ylab = NULL,
  icon_size = 1.4,
  dpi = 120,
  line_width = 0.6,
  label_size = 3.1,
  x_breaks = scales::breaks_pretty(8),
  y_breaks = scales::breaks_pretty(8),
  plot_theme = NULL
)
}
\arguments{
\item{data}{A named, nonempty list of data frames.}

\item{x, y}{Numeric columns for the horizontal and vertical axes.}

\item{icon}{Column of icon names accepted by \code{\link[=geom_icon_point]{geom_icon_point()}}.}

\item{colour}{Column whose values are names in \code{palette}.}

\item{frontier}{Logical expression selecting the rows joined by the line
and given direct labels.}

\item{label}{Column or expression containing direct text labels.}

\item{bold}{Logical column or expression selecting bold labels.}

\item{palette}{Named character vector of colours.}

\item{label_nudge}{Length-two numeric vector of x/y nudges as fractions of
each frame's x/y ranges.}

\item{x_expand}{Length-two multiplicative expansion for the x axis.}

\item{y_labels}{Label function for the y axis.}

\item{xlab, ylab}{Axis titles.}

\item{icon_size, dpi}{Icon size and rendering resolution.}

\item{line_width, label_size}{Frontier line width and direct-label size.}

\item{x_breaks, y_breaks}{Break functions for each axis.}

\item{plot_theme}{A ggplot theme. The default uses a 13-point classic theme
with black axis text.}
}
\value{
A named list of ggplots in the same order as \code{data}.
}
\description{
Applies the same line, icon, label, scale, and theme settings to each data
frame. The \code{frontier} expression and label nudges are evaluated separately
for each frame, so panels keep their own efficient strategies and ranges.
}
\examples{
df_a <- data.frame(x = 1:2, y = 1:2, icon = c("square-inset", "circle-solid"),
                   group = c("A", "B"), status = c(TRUE, TRUE),
                   label = c("A", "B"), bold = c(FALSE, TRUE))
df_b <- transform(df_a, y = y * 2)
plots <- icon_frontiers(
  list(a = df_a, b = df_b), x = x, y = y, icon = icon, colour = group,
  frontier = status, label = label, bold = bold,
  palette = c(A = "#1090F3", B = "#DF4601"), dpi = 120
)
}
\seealso{
\code{\link[=geom_icon_point]{geom_icon_point()}}
}
